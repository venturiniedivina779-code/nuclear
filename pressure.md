1. Настройка конкретного блока (в src/app/page.tsx)
Это самый удобный способ — вы меняете параметры прямо там, где вызываете компонент.

Фоновый (большой) текст: Ищите 

район 78-й строки. 

Там добавлены пропсы:
tsx

maxDist={300}   // Дистанция реакции
skewActive={25} // Сила наклона

Основной (маленький) текст: 

Район 101-й строки. 

Можете добавить такие же параметры туда, чтобы изменить его поведение.
2. Глобальные настройки по умолчанию (в src/components/ui/TextPressure.tsx)
Если вы хотите поменять базовые значения для всех текстов сразу, идите в файл компонента:

Строки 13-16: 

Тут задаются значения по умолчанию, которые используются, если вы не передали их в page.tsx:
tsx

weightInactive = 900, // Толщина в покое
weightActive = 300,   // Толщина при наведении
skewActive = 18,      // Наклон по умолчанию
maxDist = 100         // Дистанция по умолчанию

Строки 49-54: 

Здесь настраивается сама анимация GSAP (длительность duration и плавность ease).
Совет: Попробуйте поиграть со skewActive в page.tsx 

на 78 строке, поставив там, например, 50 — увидите, как сильно "завалится" фоновый текст.